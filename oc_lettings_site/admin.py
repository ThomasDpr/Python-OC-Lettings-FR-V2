"""
Admin configuration module for the oc_lettings_site application.

This module previously contained admin registrations for the original
monolithic application models (Letting, Address, Profile).
These models have been migrated to their respective apps (lettings, profiles).
"""

# from django.contrib import admin

# from .models import Letting
# from .models import Address
# from .models import Profile


# admin.site.register(Letting)
# admin.site.register(Address)
# admin.site.register(Profile)
